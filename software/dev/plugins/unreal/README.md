# Unreal Engine Integration

This package will contain Unreal-specific pipeline integrations:

- Editor Utility Widgets and Blueprints (referenced from Python)
- Python scripts for asset import/export, level management
- USD stage management (future)

## Development

Unreal's Python environment requires the Editor Scripting plugin.
Add `pipeline/` to Unreal's Python paths via Project Settings or
a startup script.
