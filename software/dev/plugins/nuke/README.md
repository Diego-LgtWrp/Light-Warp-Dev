# Nuke Integration

This package will contain Nuke-specific pipeline integrations:

- Custom gizmos and toolsets
- Python panels for shot management
- Render submission and review tools

## Development

Add `pipeline/` to NUKE_PATH or init.py so Nuke can find the
lightwarp package on startup.
